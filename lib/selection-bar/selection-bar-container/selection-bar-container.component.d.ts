import { Router } from '@angular/router';
import { SelectionBarService } from '../selection-bar.service';
export declare class SelectionBarContainerComponent {
    protected selectionBarService: SelectionBarService;
    protected router: Router;
    constructor(selectionBarService: SelectionBarService, router: Router);
}
